<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.w21g12_quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_qz"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_qz_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.W21G12_Quiz">
        <activity android:name=".googleSignInActivity"></activity>
        <activity android:name=".QuizListActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBarTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>